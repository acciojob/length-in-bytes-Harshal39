const byteSize = (str) => {
  // write your code here
	let Blob = {
		data:str,
		size:data.size();
	}
	return Blob.size;
};

// Do not change the code below
const str = prompt("Enter some string.");
alert(byteSize(str));
